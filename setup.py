from setuptools import setup,find_packages
setup(name='pyslime',
      version='0.1',
      description='Tools for analyzing output of Monte Carlo Physarum Machine models',
      packages = find_packages('pyslime'))
